import React from 'react'
import { Text, ScrollView} from 'react-native'

const Post = (props) => {
        return (
            <ScrollView>
            <Text>POST</Text>
            </ScrollView>
        )
    
}

export default Post;

